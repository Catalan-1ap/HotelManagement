<UserControl x:Class="Wpf.Views.MainTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:Wpf.ViewModels"
             mc:Ignorable="d"
             Style="{DynamicResource MaterialControl}"
             d:DataContext="{d:DesignInstance viewModels:MainTabViewModel}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <ProgressBar
            Visibility="{Binding LoadingController.IsLoading}"
            Style="{DynamicResource MaterialDesignCircularProgressBar}"
            IsIndeterminate="True">
            <ProgressBar.RenderTransform>
                <ScaleTransform ScaleX="2.5" ScaleY="2.5" />
            </ProgressBar.RenderTransform>
        </ProgressBar>

        <StackPanel
            Visibility="{Binding LoadingController.IsLoaded, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="{Binding Header}" />
            <DataGrid ItemsSource="{Binding Rooms}"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False" />
        </StackPanel>
    </Grid>
</UserControl>