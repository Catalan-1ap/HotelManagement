Write all commands and queries

Write tests for CQRS

Share readonly context. It should be same as the common one, 
but properties return IQueryable<T> instead of DbSet, also no inheritance from DbContext,
make his own interface in Application, register in Infrastructure 